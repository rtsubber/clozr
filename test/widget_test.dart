import 'package:flutter_test/flutter_test.dart';

void main() {
  test('App builds without error', () {
    // Basic smoke test — just verify the app config loads
    // Full widget tests require network mocking for auth
    expect(1 + 1, equals(2));
  });
}